apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fishbowl-tls
  namespace: default
spec:
  secretName: fishbowl-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - fishbowl.inevitable.fyi
