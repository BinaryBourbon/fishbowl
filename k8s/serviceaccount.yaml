# The Infisical secrets-operator mints tokens for this SA to authenticate as the
# shared `agent-apps-operator` identity on magpie (Kubernetes-native auth; the
# identity accepts SA name `agent-app-secrets` in any namespace). No RBAC needed.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent-app-secrets
  namespace: default
